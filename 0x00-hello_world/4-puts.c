#include <stdio.h>
#include <string.h>
/* main - This is the entry point of the program
 * This program outputs the content of a string
 * Return: Always success
 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
